description: "Motion-JPEG clips and PNG/JPEG stills on the panel"
dependencies:
  idf: ">=6.0"
  espressif/esp_new_jpeg: "^1.0.2"
  # Stills only, and only from png_image.c, so it stays out of the public headers.
  espressif/libpng:
    version: "^1.6.58"
    require: private
